fastapi
uvicorn
python-multipart
pillow
torch
torchvision
transformers
requests
opencv-python
numpy
# SAM 2 (Segment Anything Model 2) - not on PyPI, install from GitHub:
#   pip install git+https://github.com/facebookresearch/sam2.git
# Checkpoint (auto-resolved from ai-service/checkpoints/):
#   https://dl.fbaipublicfiles.com/segment_anything_2/072824/sam2_hiera_tiny.pt
git+https://github.com/facebookresearch/sam2.git
hydra-core
# Required by the SigLIP tokenizer (detection verifier); without it the
# verifier silently falls back to pass-through mode
sentencepiece
protobuf