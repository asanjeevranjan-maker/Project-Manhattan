[project]
name = "satquery-backend"
version = "0.1.0"
description = "FastAPI backend for SatQuery"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.22.0",
]

[tool.vercel]
entrypoint = "main.py:app"
